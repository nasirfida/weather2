/* General reset to remove margin and padding */
* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

/* Body styling */
body {
    font-family: 'Arial', sans-serif;
    background-color: #2692f7; /* Light blue background */
    color: white;
    display: flex;
    justify-content: center;
    align-items: center;
    height: 100vh; /* Full viewport height */
    padding: 20px;
}

/* Container for forecast cards */
.forecast-container {
    width: 80%;
    max-width: 1000px;
}

/* Header styling */
.header {
    text-align: center;
    margin-bottom: 20px;
}

/* Header text */
h1 {
    font-size: 36px;
    color: #ffcb00; /* Bright yellow color */
}

/* Grid layout for forecast cards */
.forecast-list {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(250px, 1fr));
    gap: 20px;
}

/* Individual forecast card styling */
.forecast-card {
    background-color: rgba(112, 176, 236, 0.8); /* Light blue background for cards */
    border-radius: 10px;
    padding: 20px;
    text-align: center;
    box-shadow: 2px 4px 10px rgba(0, 0, 0, 0.3);
}

/* Hover effect for forecast cards */
.forecast-card:hover {
    transform: scale(1.05); /* Slightly enlarge card on hover */
}

/* Styling for the day name */
.day {
    font-size: 18px;
    font-weight: bold;
    margin-bottom: 10px;
}

/* Weather info layout */
.weather-info {
    display: flex;
    justify-content: center;
    align-items: center;
    gap: 20px;
}

/* Icon styling */
.icon {
    width: 50px;
    height: 50px;
    border-radius: 50%;
}

/* Temperature range styling */
.temp-range {
    font-size: 18px;
    font-weight: bold;
}

/* Sunny weather icon */
.sunny {
    background-color: #ffdd00; /* Yellow color for sunny weather */
}

/* Cloudy weather icon */
.cloudy {
    background-color: #b0b0b0; /* Grey color for cloudy weather */
}
